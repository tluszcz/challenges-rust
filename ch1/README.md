A download manager is a type of software that manages the downloading of files
from the Internet, which may be built into a web browser, or as a standalone
program.

Functionality:
    - Web based GUI 
    - Splits the downloaded file into chunks and downloads with multiple 
      connections
    - Resume the download if stopped